name: Deploy website

on: 
  push:
    branches: [ main, test ]


jobs:
  test-secret:
    runs-on: ubuntu-latest

    steps:

    # Check out code
    - uses: actions/checkout@v2

    - name: Access secret
      env:
        COOL_SECRET: ${{ secrets.THIS_SECRET_DOES_NOT_EXIST }}
      run: 
        echo "$COOL_SECRET"